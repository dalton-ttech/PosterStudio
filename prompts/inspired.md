Use the supplied reference image as the main visual inspiration.

Create the requested poster using its visual character as a starting point, while allowing a freer new composition and design solution for the supplied brief.

Use the exact supplied copy and supplied brand asset(s). Do not add unrelated text or visual concepts.
